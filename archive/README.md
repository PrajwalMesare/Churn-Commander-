# 📦 Archive Folder

This folder contains files that are not part of the active codebase but may be useful for reference.

## Contents

- **customer-retention-analytics.ipynb** - Original Jupyter notebook used for model development and training (reference only)
- **config_examples/** - Example configuration files for reference

## Note

These files are preserved for historical reference but are not used in the main application. The application uses:
- `app.py` - Main Streamlit application
- `utils/` - Utility modules
- `models/` - Trained ML models
- `data/` - Customer data samples
- `requirements.txt` - Dependencies

To use this repo, only the files in the root directory and main folders are needed.
