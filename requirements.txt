# 🚀 OPTIMIZED FOR STREAMLIT CLOUD - Minimal Dependencies

# Core (Streamlit + Data)
streamlit>=1.28.0
pandas>=2.0.0,<3
numpy>=1.24.0,<2

# ML & Visualization (Keep it lean)
xgboost>=2.0.0
scikit-learn>=1.2.0
plotly>=5.14.0

# SHAP (Keep for explainability, but it's heavy)
shap>=0.42.0

# AI Integration (Gemini - lightweight)
google-generativeai>=0.3.0

# Email Services (Keep minimal)
email-validator>=2.0.0
python-dotenv>=1.0.0

# Security
cryptography>=41.0.0

# REMOVE THESE TO SPEED UP:
# - langchain (was heavy, not essential)
# - sendgrid (optional - SMTP works)
# - reportlab (optional - PDF generation)
# - python-logging-loki (optional - logging)
